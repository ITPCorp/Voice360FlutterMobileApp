// Basic smoke test. The app's full integration tests should live elsewhere
// since the real app needs Firebase/SIP services that the test runner can't
// initialise.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
